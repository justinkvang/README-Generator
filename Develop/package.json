{
  "name": "readme-generator",
  "version": "1.0.0",
  "description": "A node application that creates a README file with user input on command lines",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Justin Vang",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^6.3.1"
  }
}
